{% extends 'base.html.twig' %}

{% block title %}Rollscape - Espace Membre{% endblock %}

{% block main %}


  <div class=" card d-flex flex-column align-items-center p-3 w-50 mx-auto mt-3">
  <div class="rounded-circle  mb-3 bg-primary" style="width: 150px; height: 150px;"></div>
  <h3 class="mb-1">{{ app.user.firstname }} {{ app.user.lastname }}</h3>
  <p class="text-muted small">@username</p>

  <div class="d-flex flex-row justify-content-between w-50 gap-1 mt-3 p-3">
    <div class="text-center">
      <strong>24</strong><br><small>Uploads</small>
    </div>
    <div class="text-center">
      <strong>12</strong><br><small>Collections</small>
    </div>
    <div class="text-center">
      <strong>89</strong><br><small>Favoris</small>
    </div>
  </div>
  <div class="d-flex justify-content-center gap-2 w-100 mt-3">
    <a href="/profile/edit" class="btn btn-sm btn-outline-primary mt-4">Modifier le profil</a>
    <a href="{{path('app_account_password')}}" class="btn btn-sm btn-outline-primary mt-4">Modifier son mot de passe</a>
  </div>
</div>



{% endblock %}
