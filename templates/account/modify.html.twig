{% extends 'base.html.twig' %}

{% block title %}RollScape - Modifier mon profil{% endblock %}

{% block stylesheets %}
	{{ parent() }}
	<link rel="stylesheet" href="{{ asset('css/profiles.css') }}">
{% endblock %}

{% block javascripts %}
	{{ parent() }}
	<script src="{{ asset('js/profiles.js') }}"></script>
{% endblock %}

{% block main %}
	<div class="container w-50 mx-auto mt-5"> 
		<h2 class="ff-montserrat fw-bold fs-3">Modifier votre profil</h2>
		<div class="mt-4">
			{{ form_start(form, {
                    'attr': {
                        'novalidate': 'novalidate',
                        'data-turbo': 'false'
                    }
                }) }}
			{{ form_widget(form) }}
			{{ form_end(form) }}
		</div>
    	<p class="my-4"><a href="{{ path('app_account') }}" class="text-decoration-none">Retour à l'espace membre</a></p>
	</div>

	{# <a href="{{ path('app_profiles_index') }}">back to list</a> #}

	{# {{ include('profiles/_delete_form.html.twig') }} #}
{% endblock %}