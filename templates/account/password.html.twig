{% extends 'base.html.twig' %}

{% block title %}Rollscape - Espace Membre{% endblock %}

{% block main %}


<div class="container w-50 mx-auto">
    
    <div class="card">
        <div class="card-header">
            <h3>Modifier son mot de passe</h3>
        </div>
        <div class="card-body">

                {{form(modifyPwd, {
                    'attr': {
                        'novalidate': 'novalidate',
                        'data-turbo': 'false'
                    }
                })}}
        </div>
        <div class="card-footer">
            <a href="{{ path('app_account') }}" class="text-decoration-none">Retour à l'espace membre</a>
        </div>
    </div>
    
</div>
{% endblock %}
